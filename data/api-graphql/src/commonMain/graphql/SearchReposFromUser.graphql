query SearchReposFromUser($userName: String!, $perPage: Int!, $sort: RepositoryOrder!) {
    user(login: $userName) {
        name
        login
        avatarUrl
        repositories(first: $perPage, orderBy: $sort) {
            nodes {
                ...RepositoryFields
            }
        }
    }
}